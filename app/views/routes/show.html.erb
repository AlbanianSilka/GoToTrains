<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @route.name %>
</p>

<p>
  <strong>Станции в маршруте:</strong>
</p>

<p>
  <% if @route.railway_stations.empty? %>
    На этом маршруте станций нет
  <% else %>
  <ul>
    <% @route.railway_stations.each do |station| %>
      <li><%= station.title %></li>
      <%= form_tag update_position_railway_station_path(station.id), method: :post do %>
        <%= hidden_field_tag :route_id, @route.id %>
        <%= label_tag :number %>
        <%= number_field_tag :number, station.index(@route.id) %>
        <%= submit_tag 'Save' %>
      <% end %>
    <% end %>
  </ul>
  <% end %>
</p>

<p>
  <strong>Поезда на маршруте:</strong>
</p>

<p>
  <% if @route.trains.empty? %>
    На этом маршруте поездов нет
  <% else %>
  <ul>
    <% @route.trains.each do |train| %>
      <li><%= train.number %></li>
    <% end %>
  </ul>
<% end %>
</p>

<%= link_to 'Редактировать', edit_route_path(@route) %> |
<%= link_to 'Вернуться', routes_path %>