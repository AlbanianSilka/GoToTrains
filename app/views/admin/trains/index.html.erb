<h1><%= t('admin.trains.plural') %></h1>

<table class="table">
  <thead>
  <tr>
    <th><%= t('admin.tickets.ticket_train_number') %></th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @trains.each do |train| %>
    <%= render train %>
  <% end %>
  </tbody>
</table>

<br>

<p><%= link_to t('.new'), new_admin_train_path %></p>
<p><%= link_to t('common.back'), welcome_index_path %></p>
