<p id="notice"><%= notice %></p>

<h1><%= t('admin.trains.plural') %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('activerecord.attributes.train.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @trains.each do |train| %>
    <%= render train %>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to t('.new'), new_admin_train_path %>
<%= link_to t('common.back'), welcome_index_path %>
