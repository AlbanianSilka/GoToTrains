<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
</p>
<p>
  <strong>На станции: </strong>
  <% if @train.current_station.present? %>
    <%= @train.current_station.title %>
  <% else %>
    Не указано
  <% end %>
</p>

<tbody>
<p>
  <% if @train.cars.empty? %>
    К этому поезду не прикреплены вагоны
  <% else %>
  <ul>
    Список вагонов, которые прицеплены к этому поезду
    <% @train.cars.each do |car| %>
      <li><%= link_to "#{car.number}", car_path(car) %>: <%= car.car_type %>, нижних мест <%= car.lower_seats %>, верхних мест <%= car.top_seats %></li>
    <% end %>
  </ul>
<% end %>
</tbody>


<% if @train.route.present? %>
  <p>Поезд двигается по следующему маршруту: </p>
  <ul>
    <%= @train.route.name %>
  </ul>
<% else %>
  <p>Поезд пока не едет ни по какому маршруту</p>
<% end %>

<%= link_to 'Edit', edit_train_path(@train) %> |
<%= link_to 'Back', trains_path %>
